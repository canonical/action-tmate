{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended"],
  "schedule": ["* * * * 0,6"],
  "osvVulnerabilityAlerts": true,
  "vulnerabilityAlerts": {
    "enabled": true
  },
  "packageRules": [
    {
      "description": "Routine npm updates arrive with upstream syncs; only vulnerability fixes are raised here, and they need `npm run build` committed before merging",
      "matchManagers": ["npm"],
      "enabled": false
    }
  ]
}
